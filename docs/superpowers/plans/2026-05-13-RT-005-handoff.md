# RT-005 Handoff Prompt

Date: 2026-05-13
For: any coding agent (Codex, Claude, etc.) resuming the multi-register state-sharing task.
Status: implementation **review** — needs live gateway verification before merge.

Paste the block below as your initial prompt. It is self-contained — do not strip context lines.

---

## Prompt

You are resuming task **RT-005** for the `remempalace` plugin at `/home/derek/.openclaw/plugins/remempalace`. The implementation is already on branch `feat/RT-005-shared-runtime-state` (pushed to `origin`). Workspace gates are green (lint, build, `npm test` 601 passed / 6 skipped). The branch is held for **live gateway verification** before merging to `main`.

### Background (read first)

- Task spec + acceptance criteria: `tasks/agentic-workflow.json`, id `RT-005`, status `review`.
- Design plan: `docs/superpowers/plans/2026-05-13-multi-register-state-sharing.md`.
- Current status snapshot: `docs/current-refactor-status.md` ("Top Priority" section).
- The bug being fixed: OpenClaw calls `plugin.register()` multiple times per gateway process; before this branch, only `McpClient` was shared across closures, so `/remempalace status` reported stale `unavailable` state even after a successful probe + replay.

### What the branch already does

- Introduces `src/runtime-state.ts` with `getRuntimeState(mcp, cfg, deps)` keyed by `McpClient` instance — idempotent across `register()` calls.
- Moves `metrics`, `breakers`, `searchCache`, `kgCache`, `mempalaceRepository`, `router`, `diaryService`, `recallService`, `diaryReconciler`, `kgBatcher`, `learningService`, `heartbeat`, `memoryRuntime`, the health slot (`lastProbeAt` / `lastProbeReason` / `lastReplay` / `coldStart`), and the per-session maps into the shared container. `ensureInit` is now a singleton method on `RuntimeState`.
- Adds `resetRuntimeStateForTests()` for the test harness.
- New tests: `tests/runtime-state.test.ts` (identity equality, first-call cfg wins, distinct McpClient → distinct state, reset, single `mcp.start`, shared health slot). `tests/lifecycle.test.ts` gained two RT-005 regression tests; `afterEach` extended to call `resetRuntimeStateForTests`.

### Your job

Verify the fix actually works on the live gateway, then merge. Do **not** re-implement.

```bash
cd ~/.openclaw/plugins/remempalace
git fetch origin
git checkout feat/RT-005-shared-runtime-state
git pull --ff-only
npm ci || npm install
npm run lint && npm run build && npm test
```

All gates must stay green. Test baseline is **601 passed / 6 skipped** across 41 files. Any regression blocks merge.

Then run the **live verification** that the workspace tests cannot exercise (the bug only manifests when OpenClaw's loader is the one calling `register` multiple times in different modes):

```bash
openclaw gateway restart
sleep 5
ps -ef | grep -E "openclaw|mempalace" | grep -v grep
journalctl --user -u openclaw-gateway --since "2 minutes ago" | grep -E "config resolved|MCP client started|diary replay|persistence unverified"
```

Then send any prompt through the chat agent (Telegram bot, web UI, whatever surface the maintainer uses) so `before_prompt_build` fires. After that, ask the bot to dump `/remempalace status` **verbatim**.

### Pass criteria (all must hold)

1. Exactly **one** `mempalace.mcp_server` child process under the gateway.
2. Gateway log shows **one** `[remempalace] MCP client started` line for this run (the multiple `config resolved` lines are expected and fine).
3. Gateway log shows `[remempalace] diary replay: N/M succeeded, K failed` once and **no** `diary persistence unverified` line (or, if one appears, it must be followed by a successful retry — the probe budget is `diary.persistenceProbeTimeoutMs`, default 3000ms, configurable).
4. Bot's verbatim `/remempalace status` output shows:
   - `health: healthy`
   - `mcp_ready: yes`, `diary_write: yes`, `diary_read: yes`, `kg_writable: yes`
   - **`diary_persistent: yes`** ← this is the specific symptom the fix targets.
   - `diary.state: persistent` (or `fallback-active` if `pending_fallback > 0`, but **not** `unavailable`).
   - `last_replay: N/M succeeded` populated (not `none`).
   - `caches:` entries non-zero after at least one recall (a single prompt is enough to populate them).
5. `npm test` workspace gates still green.

### If all five pass

1. Mark `RT-005` `done` in `tasks/agentic-workflow.json`. Add a final evidence line referencing the verification timestamp + the verbatim status block. Set `active_agent: null`. Move the entries from `followups` into the `evidence` if completed.
2. Update `docs/current-refactor-status.md`: move RT-005 from "Top Priority" into "Recently Landed" with a one-line summary and the verification commit hash.
3. Commit on `feat/RT-005-shared-runtime-state` with a conventional commit message (`docs: mark RT-005 done after live verification`). Signing is configured globally — commits will be auto-signed.
4. Open a PR to `main` titled `RT-005: share runtime state across plugin register() calls`. Body should include:
   - The verbatim status block (proof-of-fix).
   - Workspace test counts.
   - A short note that this depends on RT-006 (already on `main`).
5. If the maintainer is the one running this prompt and wants you to merge directly: `git checkout main && git merge --no-ff feat/RT-005-shared-runtime-state && git push origin main`. Otherwise leave the PR open for review.

### If any pass criterion fails

**Do not** flail at the implementation blindly. Capture diagnostic evidence first:

```bash
journalctl --user -u openclaw-gateway --since "10 minutes ago" -n 5000 > /tmp/rt005-gateway.log
# get the verbatim /remempalace status from the bot, save to /tmp/rt005-status.txt
ls -la ~/.mempalace/palace/diary/
ls -la ~/.openclaw/plugins/installs.json
```

Then diagnose against the symptom signatures already documented in `TROUBLESHOOTING.md`:

- "Agent tools (...) don't appear in the model's tool list" — manifest / installs.json staleness, allowlist.
- "Diary persistence stuck at `tool-present` / `fallback-active` after a fresh start" — probe timeout (RT-006); maybe bump `diary.persistenceProbeTimeoutMs` higher.
- "`/remempalace status` shows fresh state but the gateway log says replay succeeded" — this is the multi-register bug RT-005 targets. **If you still see this symptom after the branch is deployed**, the fix is incomplete — open the gateway log and confirm `getRuntimeState` is returning the same instance across closures (you can add a temporary `logger.info` in `runtime-state.ts` to log the instance id; remove before commit).

Report back with:
- The five pass criteria, each marked pass/fail.
- If any failed, the diagnostic captures and your hypothesis for which bullet of the RT-005 plan was missed.
- Whatever commit(s) you produced.

### Constraints

- **Do not push** unless every gate is green and you've at minimum opened a PR (or the maintainer explicitly approved a direct push).
- **Do not modify `main`** until the branch is merged through normal flow.
- **Signing is mandatory** — `commit.gpgsign=true` is set globally; do not pass `--no-gpg-sign`. If signing fails, fix the underlying issue (key permissions, agent) rather than skipping.
- Keep diffs focused; no drive-by refactors.
